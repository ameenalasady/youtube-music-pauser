# Ignore the Node.js modules directory, which contains project dependencies.
# These are typically installed via `npm install` and should not be committed to version control.
node_modules/

# Ignore the 'dist' directory, which is used for temporary build files.
# These files are generated by the build script and are not part of the source code.
dist/

# Ignore the 'release' directory, which contains the final ZIP files for distribution.
# These are generated by the build script.
release/

# Ignore any files ending with .zip, which are the packaged extension files.
*.zip