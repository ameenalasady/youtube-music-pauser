{
  "name": "ytm-pauser",
  "version": "1.0.1",
  "description": "Automatically pauses YouTube Music when a YouTube video starts playing.",
  "main": "src/background.js",
  "scripts": {
    "build": "node ./scripts/build.js",
    "release": "npm run build && git commit -am \"build: v%npm_package_version%\" && git tag v%npm_package_version%",
    "postversion": "git push && git push --tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "archiver": "^5.3.1",
    "fs-extra": "^11.1.0"
  }
}
